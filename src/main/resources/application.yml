spring:
  application:
     name: fluxmartApi
  datasource :
    url: jdbc:mysql://localhost:3306/sql_store?createDatabaseIfNotExist=true
    username: root
    password: Kimz@9600
  jpa:
    show-sql: true
  jwt:
    secretKey: ${JWT_SECRET}
    refreshExpiration: 604800
    accessExpiration: 86400

stripe :
#  secretKey : ${STRIPE_SECRET_KEY}
#  webhooksecretkey: ${STRIPE_WEBHOOK_SECRET_KEY}


websiteUrl : http://localhost:4040